You are an adversarial security/correctness reviewer for YAGI Workshop Phase 1.2 (just completed). The build is clean (0 errors, 0 warnings); your job is to find real bugs the build cannot catch.

PHASE 1.2 SCOPE (review ONLY these areas — Phase 1.1 base is out of scope):
- src/lib/og-unfurl.ts — SSRF guard, redirect handling, content-type / size caps
- src/lib/email/new-message.ts — recipient fan-out from notifyNewMessage()
- src/lib/supabase/service.ts — service-role admin client
- src/app/api/unfurl/route.ts — POST endpoint auth + rate
- src/app/[locale]/app/projects/new/* — 3-step wizard + createProject Server Action
- src/app/[locale]/app/projects/[id]/* — detail page, transitionStatus, ref-actions, thread-actions
- src/app/[locale]/app/admin/* — yagi_admin role gate, cross-workspace list
- src/app/[locale]/app/settings/* — profile/workspace/team forms + 5 actions
- src/components/project/reference-uploader.tsx — drag-drop + URL tab
- src/components/project/reference-grid.tsx — signed URL rendering
- src/components/project/thread-panel.tsx — Realtime subscription + visibility toggle
- supabase storage policies (avatars private, refs_insert_authorized — confirm coverage)

SPECIFIC ADVERSARIAL FOCUS (rank findings HIGH/MEDIUM/LOW; cite file:line; one-line fix each):

1. **RLS visibility leaks** — can a workspace_member with role='member' (non-yagi_admin, non-ws_admin) SELECT a thread_message with visibility='internal'? Can they enumerate other workspaces' projects by manipulating the workspace filter on /admin/projects (a regular member should be redirected at the layout, but verify the action layer too)?

2. **Avatar upload path safety** — in src/app/[locale]/app/settings/profile-form.tsx, the upload path is `{auth.uid()}/{uuid}.{ext}`. Can a user manipulate the upload to land at `{otherUserId}/avatar.jpg`? Check the storage RLS policy on the avatars bucket and the form's path construction.

3. **Reference uploader path safety** — in ref-actions.ts addReference, the storage path is `{projectId}/{uuid}.{ext}`. Confirm the new refs_insert_authorized RLS policy actually validates project membership (not just project existence). What if projectId is forged?

4. **Email fan-out leakage** — does notifyNewMessage ever email someone NOT in the workspace_members table for the project's workspace, or NOT a yagi_admin? Does it ever skip the author exclusion (sending the author their own message)? What if a user has multiple workspace_members rows due to bug?

5. **OG unfurl SSRF** — beyond the 127/10/172.16-31/192.168 IPv4 check: can an attacker bypass via IPv6 (::1, fc00::/7), DNS rebinding (TTL=0 hostname resolving to public then private), 0.0.0.0, link-local 169.254.x.x, or URL-encoded hostnames? Verify the redirect: "follow" behavior — does each redirect re-validate the hostname?

6. **Status transition state machine** — in transitionStatus, the ALLOWED map is duplicated (per checkpoint.md "ALLOWED map duplicated"). Is the second copy in sync with the first? Can a client-side discrepancy let a member fire an admin-only transition by spoofing the form?

7. **Service-role client misuse** — grep for `createServiceClient` usages. Is it ever called from a code path that doesn't have an upstream auth + role check? Service-role bypasses RLS entirely.

8. **Realtime channel leakage** — channel naming is `project:{projectId}:thread`. If a non-member subscribes (knows the projectId), do they receive payloads? Realtime is gated by RLS on the underlying table SELECT — verify the postgres_changes filter gives them nothing.

9. **transitionStatus error code** — code returns `{ error: "forbidden" }` for invalid transitions. Does the action verify that auth.uid() actually has the role required for the source→target transition (not just that the transition is in ALLOWED)?

10. **Settings inviteMember stub** — confirms the stub returns `{ error: "not_implemented" }` and does NOT touch the workspace_invitations table. A naive partial implementation is worse than no implementation.

Output format per finding:
```
[SEVERITY] file:line — short title
Exploit: <one sentence>
Fix: <one sentence>
```

If you find NO real bugs in a focus area, say so explicitly. Do not fabricate findings to fill quota. Quality over quantity.
