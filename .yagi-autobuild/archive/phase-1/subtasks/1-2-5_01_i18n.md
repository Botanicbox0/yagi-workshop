# Phase 1.2.5 / Subtask 01 — i18n keys

**status:** pending
**assigned_to:** executor_haiku_45
**created:** 2026-04-22
**parallel_group:** A
**spec source:** `.yagi-autobuild/phase-1-2-5-spec.md` §"Subtasks / 01"

---

## Executor preamble

1. Read ONLY this subtask file + `/CLAUDE.md`. Do NOT read other subtask files, task plans, or the full phase spec.
2. Working directory: `C:\Users\yout4\yagi-studio\yagi-workshop`.
3. Allowed tools: Read, Edit, Write, Grep.
4. Both `messages/ko.json` and `messages/en.json` already exist with 15 namespaces. Do NOT create new namespaces; add keys to the existing `projects`, `refs`, and `threads` namespaces.

## Scope (verbatim from spec)

Add to both `messages/ko.json` and `messages/en.json`:

**`projects` intake mode keys:**
- `intake_mode_label`
- `intake_mode_brief_title`, `intake_mode_brief_desc` — "기획안을 이미 갖고 있어요" / "I already have a brief"
- `intake_mode_proposal_title`, `intake_mode_proposal_desc` — "YAGI가 제안해주길 원해요" / "I want YAGI to propose"
- `proposal_goal_label`, `proposal_goal_ph`
- `proposal_audience_label`, `proposal_audience_ph`
- `proposal_budget_range_label`, `proposal_budget_range_ph`
- `proposal_timeline_label`, `proposal_timeline_ph`

**`refs` (or whichever namespace existed in Phase 1.2 — verify name first) video/pdf keys:**
- `ref_type_video`, `ref_type_pdf`
- `ref_video_upload_ph` — "비디오 파일 드래그 또는 클릭 (MP4 · MOV · WebM · 최대 500MB)"
- `ref_pdf_upload_ph` — "PDF 드래그 또는 클릭 (최대 25MB)"
- `ref_video_url_ph` — "YouTube · Vimeo · TikTok · Instagram URL 붙여넣기"
- `ref_video_unsupported_format` — "이 브라우저에서 재생되지 않을 수 있습니다 (MP4 권장)"
- `ref_size_too_large_video`, `ref_size_too_large_pdf`
- `ref_duration_label`, `ref_pages_label`

**`threads` attachment keys:**
- `attachment_add`, `attachment_remove`
- `attachment_uploading`, `attachment_failed`
- `attachment_size_limit` — "파일은 하나당 최대 500MB"

## Style

- Korean: 존댓말 (formal polite), editorial, short.
- English: editorial, sentence case, no exclamation marks.

## Acceptance

- Both JSON files valid (test by running `node -e "require('./messages/ko.json'); require('./messages/en.json')"` — should succeed silently).
- No key present in one locale but missing from the other (Grep both files for each new key — counts must match).
- File diff is keys-only — no reformatting of existing namespaces.

## Result file (`results/1-2-5_01_i18n.md`)

```markdown
# Phase 1.2.5 / Subtask 01 result
status: complete | needs_kill_switch | blocked
files_modified: [messages/ko.json, messages/en.json]
keys_added_count: <N>
namespace_used_for_video_pdf: <"refs" | "references" | other actual name>

## Notes
<any deviations or blockers>
```
